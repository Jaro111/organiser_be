version: "3"
services:
  frontend:
    build:
      context: ./organiser_fe/  # Ensure this points to the correct directory
      dockerfile: Dockerfile      # Specify your Dockerfile if not named "Dockerfile"
    container_name: frontend-app
    ports:
      - "3000:3000"               # Expose the React app on port 3000
    environment:
      - REACT_APP_API_URL=http://localhost:5000  # Adjust if needed
    volumes:
      - /c/Users/jarok/Desktop/jsProjects/organiser/organiser_fe/src:/app  # Docker-compatible path
    networks:
      - app-network